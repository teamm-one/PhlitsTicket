@model IEnumerable<Models.Models.Airport>;

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr class="text-center">
            <th>
               #
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="text-center">
            <td>
                @Html.DisplayFor(modelItem => item.AirportId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td class="text-center">
                <a href="/Airport/Details/@item.AirportId" class="btn btn-outline-success py-1 me-2">Details</a>
                <a href="/Airport/Edit/@item.AirportId" class="btn btn-outline-primary py-1 me-2">Edit</a>
                <a href="/Airport/Delete/@item.AirportId" class="btn btn-outline-danger disabled py-1 me-2">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
